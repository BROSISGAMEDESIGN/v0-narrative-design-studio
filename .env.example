# Supabase (Project Settings → API). Publishable key works as NEXT_PUBLIC_SUPABASE_ANON_KEY.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Single shared row in engine_projects (default matches SQL seed).
NEXT_PUBLIC_ENGINE_PROJECT_SLUG=main
